# Core (required).
# NOTE: this environment ships scipy 1.7.3 which needs numpy<1.23.
#       Keep numpy pinned accordingly, otherwise pip may upgrade numpy and break scipy.
numpy>=1.22,<1.23
pandas>=1.4
scipy>=1.7
matplotlib>=3.5
PyYAML>=5.4
openai>=1.30

# Real-market data (optional; offline synthetic world is the default)
akshare>=1.12

# Conversational UI (optional)
streamlit>=1.30
jinja2>=3.1.2     # required by pandas Styler (colored index table)

# Report-PDF deep factor mining (optional, for 研报PDF解析)
pypdf>=3.0

# Dev
pytest>=7.0
